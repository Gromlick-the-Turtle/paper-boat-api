import { faker } from '@faker-js/faker';

import Institution from '#models/Institution';

export default {
    model: Institution,
    route: 'institution',
    id: 1,
    create: {},
    update: {},
    generate: () => ({})
}