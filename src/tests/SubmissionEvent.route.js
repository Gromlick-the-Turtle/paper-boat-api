import { faker } from '@faker-js/faker';

import SubmissionEvent from '#models/SubmissionEvent';

export default {
    model: SubmissionEvent,
    route: 'submissionevent',
    id: 1,
    create: {},
    update: {},
    generate: () => ({})
}