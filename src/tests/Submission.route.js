import { faker } from '@faker-js/faker';

import Submission from '#models/Submission';

export default {
    model: Submission,
    route: 'submission',
    id: 1,
    create: {},
    update: {},
    generate: () => ({})
}