import { faker } from '@faker-js/faker';

import CustomForm from '#models/CustomForm';

export default {
    model: CustomForm,
    route: 'customform',
    id: 1,
    create: {},
    update: {},
    generate: () => ({})
}